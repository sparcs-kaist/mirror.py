import mirror


def crontab(user: str, config: str) -> None:
    """Generate crontab entries for the mirror daemon.

    Args:
        user(str): System user to run the cron job as.
        config(str): Path to the mirror configuration file.
    """
    pass